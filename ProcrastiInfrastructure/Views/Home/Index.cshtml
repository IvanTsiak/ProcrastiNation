@using ProcrastiDomain.Model;
@model ProcrastiInfrastructure.Models.DashboardViewModel

@{
    ViewData["Title"] = "Головна";
}

<div class="banner">
    <h2 class="text-2xl font-bold opacity-90 mb-2 drop-shadow-sm">Витрачено всіма:</h2>
    <h1 class="text-6xl font-black text-rose-500 drop-shadow-md">
        @Model.GlobalLossAmount <span class="text-3xl text-white opacity-90 tracking-wide">хв</span>
    </h1>
</div>

<div class="bg-surface border border-secondary/50 rounded-xl overflow-hidden shadow-sm">

    <div class="px-5 py-3 border-b border-secondary/50 bg-background/60">
        <h3 class="font-bold text-text text-base">Як там інші поживають?</h3>
    </div>

    <div class="flex flex-col">
        @foreach (var log in Model.RecentLogs)
        {
            <partial name="_LogCard" model="log" />
        }
    </div>
</div>